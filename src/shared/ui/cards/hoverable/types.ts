/**
 * @interface HoverableCardProps
 * @property {string} icon - CSS background-image class for the icon
 * @property {string} title - Accessible title for the card
 */
export interface HoverableCardProps {
    icon: string;
    title: string;
}
