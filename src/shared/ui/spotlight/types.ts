/**
 * @interface SpotlightProps
 * @property {string} [fill] - Fill color for the ellipse; defaults to "#ca38b7"
 * @property {string} [className] - Additional class names for sizing and positioning
 */
export interface SpotlightProps {
    fill?: string;
    className?: string;
}
