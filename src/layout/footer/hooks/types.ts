/**
 * @interface IMessageFormData
 * @property {string} name - Sender's full name
 * @property {string} email - Sender's email address
 * @property {string} message - Message body text
 */
export interface IMessageFormData {
    name: string;
    email: string;
    message: string;
}
